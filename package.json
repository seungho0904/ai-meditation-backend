{
  "name": "zenit-workspace",
  "private": true,
  "description": "Convenience scripts; real Next app lives in frontend/",
  "scripts": {
    "setup": "bash scripts/setup-env.sh",
    "smoke": "bash scripts/smoke.sh",
    "dev": "npm run dev --prefix frontend",
    "dev:api": "bash scripts/run-api-dev.sh",
    "dev:stack": "concurrently -k -n api,web -c blue,green \"bash scripts/run-api-dev.sh\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
